
<%= @announcement.each do |user| %>

  <div class = "content">
    <h1><%= user.title%></h1>
    <p>
      Escrito por: <%= user.user.email %>
    </p>
    <div class = "text_content">
      <%= user.body %>
    </div>
    <div class = "text_content">
      <p>Fecha de creacion:  <%= user.created_at.strftime("%d del mes %B del año %Y") %></p>
    </div>
    <%= link_to "Delete", user, method: :delete,class:"btn" %>
    <%= link_to "Visit", user,class:"btn" %>
    <%= link_to "Edit", edit_announcement_path(@announcement),class:"btn" %>rails
  </div>

<% end %>


